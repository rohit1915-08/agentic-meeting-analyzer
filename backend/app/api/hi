1. AMD ZenDNN (Zen Deep Neural Network Library)

Since Ollama is doing the heavy lifting on your processor, we can integrate AMD's ZenDNN. This is an official AMD software toolkit built to accelerate AI inference directly on Ryzen and EPYC CPU cores.

The Pitch: "We recognized that edge devices might not always have an NPU, so we optimized our application's CPU execution path using AMD ZenDNN, maximizing the matrix multiplication throughput on standard Ryzen architecture."

2. AMD uProf (Advanced Telemetry)

We are still ripping out that basic psutil library. We will hook up the AMD uProf command-line tool to pull real, granular hardware metrics.

The Pitch: "To prove our optimizations worked, we integrated AMD uProf to monitor the live CPU power draw and cache hit rates during document embedding."
